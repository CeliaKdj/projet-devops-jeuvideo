services:
  two-ships:
    build:
      context: ./games/Two-Ships-ITN
      dockerfile: Dockerfile
    ports:
      - "8081:80"
    restart: always

  space-invaders:
    build:
      context: ./games/Space-Invaders
      dockerfile: Dockerfile
    ports:
      - "8082:80"
    restart: always